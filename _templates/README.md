# _templates/ — заводские образцы

Чистые стартовые `state.json` и `gallery.json` для нового прохождения курса.
`scripts/reset.sh` копирует их в корень при сбросе форка. НЕ редактируется учеником.
Прогресс ученика живёт в корневых `state.json`/`gallery.json` (versioned в git).
