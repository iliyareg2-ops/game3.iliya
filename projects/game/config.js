/*
  config.js — «настройки игры». Единственный файл, который наставник правит,
  собирая игру ребёнка (плюс замена картинок: hero.png, scenes/, items/).
  Движок (assets/core.js) эти числа только читает.

  Пути к картинкам — относительны projects/game/index.html.
*/

const CONFIG = {
  // --- размер игрового поля (кадры комикса 900x900, квадрат) ---
  canvasW: 900,
  canvasH: 900,

  // --- герой: какой спрайт, какого размера, насколько прозрачный ---
  spriteFile: "hero.png",        // спрайт героя ребёнка (PNG с прозрачностью)
  heroScale: 0.28,               // размер героя = доля высоты кадра (стоячий спрайт)
  heroAlpha: 1.0,                // непрозрачный (для полупрозрачного спрайта — напр. 0.78)
  edgePad: 0.42,                 // насколько центр героя подпускается к краю кадра

  // --- движение: разгон, торможение, скорость («как герой летает») ---
  accel: 0.55,                   // разгон при нажатой стрелке, px/кадр^2
  friction: 0.92,                // трение: скорость * friction каждый кадр (0..1)
  maxSpeed: 4.5,                 // ограничение скорости, px/кадр

  // --- наклон «летит» ---
  tiltMax: 0.30,                 // макс. крен в радианах (~17°)
  tiltEase: 0.12,                // плавность доводки крена к цели (0..1)

  // --- переход между сценами ---
  exitZone: 0.16,                // зона выхода у правого края (доля ширины кадра)
  transitionCode: "KeyE",        // физ. клавиша перехода (e.code — не зависит от раскладки)

  // --- испытание «Проявитель» (fog) ---
  fogColor: "rgba(40,44,60,0.92)", // цвет/плотность слоя тумана
  fogThreshold: 0.82,            // доля рассеянного тумана для прохождения (0..1)
  fogSampleStep: 12,             // шаг сетки при подсчёте заполнения (px; меньше — точнее)

  // --- испытание «Догонялки» (chase) ---
  chaseFleeRange: 220,           // радиус, в котором награда начинает убегать (px)
  chaseFleeAccel: 0.7,           // сила убегания
  chaseFriction: 0.94,           // трение награды
  chaseMaxSpeed: 6,              // макс. скорость награды (px/кадр; < maxSpeed героя)

  // --- награда; в коде называется reward ---
  rewardSize: 110,               // высота награды-спрайта на экране (px); ширина — по пропорции
  rewardWord: "",                // слово артефакта ученика для подсказок (звезду/монету/ключ). Пусто → "награда"
  pickupRange: 130,              // дистанция подбора награды клавишей E, px
  pickupCode: "KeyE",            // клавиша подбора (та же, что переход — по контексту)
  pickupAnimMs: 420,             // длительность анимации подбора, мс

  // --- инвентарь (полоса поверх низа кадра) ---
  invHeight: 82,                  // высота полосы, px
  invMargin: 12,                  // отступ от нижнего края canvas, px
  invSlotSize: 56,                // размер слота, px
  invSlotGap: 14,                 // отступ между слотами, px
  invBg: "rgba(28,30,42,0.55)",   // фон панели (полупрозрачный — фон сцены просвечивает)
  invSlotEmpty: "rgba(255,255,255,0.10)", // пустой слот
  invSlotEdge: "rgba(255,216,107,0.55)",  // рамка пустого слота

  // --- сцены: кадры комикса ребёнка по порядку истории ---
  // mechanic — имя испытания на кадре (см. assets/mechanics/). Без mechanic —
  // кадр без испытания, переход открыт сразу (старт / финал).
  // reward — спрайт награды кадра (items/<имя>.png).
  // comic_frame — из какого кадра комикса сделан этот фон
  //   (напр. "../comics/scenes/comic_call.png"). Необязательное поле: движок его
  //   не читает, оно хранит связь «фон ← кадр», чтобы порядок сцен можно было
  //   сверить с комиксом. Порядок SCENES = порядок кадров истории.
  // В заготовке один кадр-плейсхолдер; наставник заполняет на шагах 2.1–2.4.
  SCENES: [
    { image: "scenes/placeholder.png" },
  ],

  // --- активные модули движка (ядро core.js читает) ---
  // Старт Уровня 2 — пусто: герой просто стоит на холсте, ничего не умеет.
  // Наставник наращивает по шагам курса (см. level_2_*.md):
  // 2.1 +control (полёт) +transitions (переход «Дальше»), 2.4 +inventory, 2.5 +overlays.
  // Подключение модуля = <script> в index.html + имя в этом списке.
  MODULES: [],

  // --- пример-механика «dodge_collect» (леталка: лови цели, уворачивайся от помех) ---
  // Это образец «своей механики» (см. assets/mechanics/README.md). Числа —
  // отправная точка; ученик подставляет свою тему (что за «цель» и «помеха»).
  dodgeGoal: 10,             // сколько целей собрать для прохождения
  dodgeTargetSize: 70,       // высота спрайта цели на экране, px
  dodgeTargetSpeed: 2.4,     // скорость целей справа налево, px/кадр
  dodgeTargetEvery: 900,     // интервал появления целей, мс
  dodgeHazardSize: 64,       // диаметр помехи, px
  dodgeHazardSpeed: 3.2,     // базовая скорость помех, px/кадр
  dodgeHazardEvery: 1100,    // интервал появления помех, мс
};
